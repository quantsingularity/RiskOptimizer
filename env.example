# ----------------------------------------------------------------
# RiskOptimizer Environment Variables Example
# ----------------------------------------------------------------

# This file contains all environment variables used by the project,
# including those for local development and Docker Compose setup.
# Copy this file to .env and fill in your desired values.

# ----------------------------------------------------------------
# 1. Backend Configuration (Used by code/backend/config.py)
#    These are the defaults for running the backend directly.
# ----------------------------------------------------------------

# Database Connection (for local development outside of Docker)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=riskoptimizer
DB_USER=postgres
DB_PASSWORD=postgres

# API Server Configuration
API_HOST=0.0.0.0
API_PORT=5000
DEBUG_MODE=True

# Blockchain Configuration
BLOCKCHAIN_PROVIDER=http://localhost:8545
PORTFOLIO_TRACKER_ADDRESS=0x0000000000000000000000000000000000000000
RISK_MANAGEMENT_ADDRESS=0x0000000000000000000000000000000000000000

# ----------------------------------------------------------------
# 2. Docker Compose Overrides (Used by docker-compose.yml)
#    These variables override the defaults for the containerized services.
# ----------------------------------------------------------------

# PostgreSQL Password (used by the 'postgres' service)
# Default in docker-compose.yml is 'riskoptimizer123'
POSTGRES_PASSWORD=riskoptimizer123

# Grafana Admin Password (used by the 'grafana' service)
# Default in docker-compose.yml is 'admin123'
GRAFANA_PASSWORD=admin123

# ----------------------------------------------------------------
# 3. Frontend Configuration (Used by web-frontend)
#    This is the API URL the frontend will connect to.
# ----------------------------------------------------------------

# API URL for the web-frontend to connect to the backend API.
# For local development (backend running on port 5000), use:
# VITE_API_URL=http://localhost:5000
# For Docker Compose (internal network), this is set to http://api:8000 in the compose file.
VITE_API_URL=http://localhost:5000
