---
apiVersion: v1
kind: Service
metadata:
  name: riskoptimizer-frontend
  namespace: riskoptimizer
  labels:
    app: riskoptimizer-frontend
    component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: riskoptimizer-frontend
    component: frontend
