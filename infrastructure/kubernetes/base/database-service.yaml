---
apiVersion: v1
kind: Service
metadata:
  name: riskoptimizer-database
  namespace: riskoptimizer
  labels:
    app: riskoptimizer-database
    component: database
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    app: riskoptimizer-database
