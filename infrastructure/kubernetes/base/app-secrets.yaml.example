# Kubernetes Secrets Example
# DO NOT commit actual secrets to version control
# Use external secret management (Vault, AWS Secrets Manager, etc.)

apiVersion: v1
kind: Secret
metadata:
  name: riskoptimizer-secrets
  namespace: riskoptimizer
type: Opaque
stringData:
  # Database credentials
  database-username: "REPLACE_WITH_ACTUAL_USERNAME"
  database-password: "REPLACE_WITH_ACTUAL_PASSWORD"
  
  # API keys
  api-key: "REPLACE_WITH_ACTUAL_API_KEY"
  
  # JWT secret
  jwt-secret: "REPLACE_WITH_ACTUAL_JWT_SECRET"
  
# RECOMMENDED: Use external-secrets operator with Vault or cloud providers
# See secrets-management.yaml for External Secrets configuration
